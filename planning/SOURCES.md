# Consumed-source provenance — reference only

Out of the read-path (the navigation provenance rule). The structured artifact is
the entry; this records where its inputs came from. Nothing here is a "read first".

## bucket/resetprop-rs-injectrc-ports/  (cut 2026-06-16)

```
dropped plan  → bucket/resetprop-rs-injectrc-ports/PLAN.md   (decomposer input)
                loose authoring copy lived outside this repo (author plane),
                not tracked here.
external port sources (cited as external_refs; sibling repos, NOT vendored):
  injectrc  @ c3fb624   /home/president/Git-repo-success/injectrc
  ReZygisk  @ 093dfaf   /home/president/Git-repo-success/ReZygisk
```

The plan's own text cited a stale injectrc rev `5ec1cff` and a wrong path
(`Phantom-veil/injectrc`); the verified source is `injectrc@c3fb624` above.
